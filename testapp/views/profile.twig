{% extends "layout.twig" %}

{% block content %}

    <div class="container">
        <h1>{{ header }}</h1>
        {% if msg %}
            <p class="alert alert-success">{{ msg }}</p>
        {% endif %}
        {{ form|raw }}
    </div>

{% endblock %}
